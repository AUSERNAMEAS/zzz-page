import type { ActionOption } from '../types/action'

/** Structural data only — copy lives in i18n JSON (`actions.*`). */
export const actionOptions: ActionOption[] = [
  { id: 'TeamBuilder', href: '#TeamBuilder' },
  { id: 'CharacterBuilder', href: '#CharacterBuilder' },
  { id: 'GameMechanics', href: '#GameMechanics' },
]
